[project]
name = "agentic-dags"
version = "0.1.0"
description = "Agentic DAG templates with MCP and Prefect"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai-agents>=0.1.0",
    "prefect>=2.14.0",
    "pydantic>=2.6.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
dev-dependencies = []
